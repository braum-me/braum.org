---
interface Props {
  label: string;
  centered?: boolean;
}
const { label, centered = false } = Astro.props;
---

<div class:list={["eyebrow", centered && "justify-center"]}>
  <span>{label}</span>
</div>
